import { css } from '@emotion/react';
import { colors } from './styles/color.style';

const AppStyle = css`
  text-align: center;
  height: 600px;
  width: 450px;
  overflow: auto;
  background-color: ${colors.gray};
`;

export { AppStyle };
